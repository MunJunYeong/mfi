type Query {
  users: [User]
  user(id: ID!): User
}

type Mutation {
  createUser(createUserInput: CreateUserInput): User
}

type User {
  id: Int
  name: String
  product: Product
}
"""
Test comment
"""
input CreateUserInput {
  name: String
}