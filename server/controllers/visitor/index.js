
const { visitor: visitorService } = require('../../service');

const postIp = async (req, res, ip) => {
    // console.log(req)
    // const ip = req.clientIp;
    // const result = await visitorService.createIp(ip);
    // console.log(result);
    // res.send(result);
    return;
}

module.exports = {
    postIp
}