type User  {
  userIdx: ID!
  id: String!
  pw: String!
  nickName: String!
  email: String!
  created: String!
  status: String
  role: String!
}

# input CreateUserInput {
#   # Example field (placeholder)
# }

type Query {
  findId(email: String): [User]
  # getUser(): [User]
}

# query getUser(){
#   user
# }

query findId($email: String) {
  user(email: $email){
    id
  }
}

# type Mutation {
#   signin
# }

